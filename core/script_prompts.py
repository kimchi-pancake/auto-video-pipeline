"""
core/script_prompts.py
=======================
story.txt DSL 형식과 "사연 채널" 스토리텔링 규칙을 못박은 대본 생성 프롬프트.
gui/claude_panel.py(수동 복사용)와 core/ai_script_generator.py(API 자동 생성용)
양쪽에서 같이 씁니다 — 프롬프트가 둘로 갈라지면 결과 포맷이 어긋날 수 있어
한 곳에서만 관리합니다.

50~70대 시청자 대상 사연 채널 전문 작가 페르소나 + 시청 유지(retention) 규칙은
사용자가 직접 작성한 프롬프트를 그대로 반영한 것입니다 (2026-07-17).
"""

from __future__ import annotations

_PERSONA = """\
당신은 한국의 50~70대 시청자를 대상으로 하는 유튜브 사연 채널 전문 작가다.
목표는 "감동"이 아니라 "끝까지 보게 만드는 것"이다."""

_BGM_GUIDE = """\
[BGM 고르기 — 반드시 이야기 분위기에 맞춰 아래 6개 중 하나를 골라라. 매번
"simple.mp3"로 똑같이 고르지 말고, 이 이야기의 톤에 맞는 걸 실제로 판단해서 골라라]
  - sad.mp3          : 갈등, 배신감, 서운함, 슬픈 전개가 중심일 때
  - piano.mp3         : 잔잔하고 담담한 서사, 회상 장면이 많을 때
  - ambient_piano.mp3 : 긴장감 있는 갈등, 무거운 분위기
  - lovely.mp3        : 화해, 훈훈한 재회, 따뜻한 결말
  - happy.mp3         : 해피엔딩, 밝고 경쾌하게 마무리될 때
  - simple.mp3         : 위 어디에도 뚜렷하게 안 맞는 무난한 이야기일 때만(기본값 아님)"""

_TOPIC_RULES = """\
[주제]
매번 이전 이야기와 완전히 다른 소재를 골라줘 (이전에 쓴 적 없을 법한 조합으로).
가능한 소재: 며느리와 시어머니의 갈등 / 아들과 부모 사이의 거리감 / 손주 문제 /
형제간 재산 문제 / 은퇴 후 외로움 / 오랫동안 참아온 부부 관계 / 부모 부양 /
명절 갈등 / 노후 자금 / 가족 사이의 오해

중요:
- 특정 인물을 무조건 악역으로 만들지 않는다. 양쪽 모두 이해할 수 있게 만들되,
  주인공은 억울한 감정을 느껴야 한다.
- 댓글에서 의견이 갈릴 수 있는 구조로 만든다."""

_STYLE_RULES = """\
[문체]
- 실제 사람이 경험담을 들려주는 느낌으로 쓴다. 지나치게 교훈적이거나 설교하지 않는다.
- 설명보다 대화와 상황 묘사를 많이 사용한다. 감정을 과장하지 말고 현실적으로 표현한다.
- 시니어 시청자가 쉽게 이해할 수 있는 문장만 사용한다.
- 나레이션은 반드시 존댓말(-습니다/-였습니다체)로 쓴다.
- 신조어, 인터넷 밈, 지나치게 젊은 말투("~함", "ㄹㅇ", "찐" 등)는 절대 쓰지 않는다.
- 문장은 자연스러운 한국어로, 지나치게 빠르게 전개하지 않는다 — 상황을 충분히 설명하고
  다음 문장으로 넘어간다."""

# 소재 분류 고정 목록 — 나중에 채널별 성과(조회수/시청지속시간)를 카테고리별로
# 비교 분석하려면 매번 자유 텍스트가 아니라 같은 라벨을 써야 집계가 됩니다.
CATEGORIES = [
    "고부갈등", "부모자식갈등", "손주문제", "형제유산", "노년외로움",
    "부부갈등", "부모부양", "명절갈등", "노후자금", "가족오해",
    "반전사연", "재회",
]

_CATEGORY_RULE = f"""\
[카테고리]
아래 목록 중 이번 이야기에 가장 가까운 것 딱 하나만 골라 CATEGORY: 에 그대로 적어줘
(목록에 없는 새 이름을 만들거나 여러 개 적지 마): {', '.join(CATEGORIES)}"""

LONG_SCRIPT_PROMPT = f"""\
{_PERSONA}

아래 형식 그대로, 최소 22개 이상의 [SCENE] 블록으로 구성된 유튜브 롱폼 사연 영상
대본을 작성해줘. 전체 대사 분량은 최소 4500자 이상, 실제 낭독 시간 기준 8분 이상이
되도록 충분히 길고 상세하게 써줘. 요약하거나 짧게 끝내지 말고, 소주제 하나하나를
충분히 설명해줘.

**각 SCENE의 나레이터 대사는 반드시 200자 이상(체감상 3~5문장)으로 써라.** 한두
문장으로 짧게 끝내는 SCENE이 하나라도 있으면 안 된다 — 씬 개수만 채우고 대사를
짧게 쓰는 건 반칙이다. (22개 × 200자 이상 = 총 4500자 이상이 자연히 채워지도록
씬 하나하나를 충실하게 채워라.) 상황 설명, 인물의 속마음, 주변 묘사, 대화를
섞어서 각 장면을 충분히 길게 늘여써라.

[핵심 목표]
- 첫 15초 안에 강한 갈등, 배신감, 서운함, 충격적인 상황을 제시한다.
- 시청자가 "나라면 어떻게 했을까?"를 계속 생각하게 만든다.
- 노년층이 실제로 겪을 법한 현실적인 이야기여야 한다.
- 지나친 막장이나 범죄보다 인간관계의 미묘한 감정을 활용한다.
- 영상 후반부에는 예상하지 못한 반전이나 새로운 사실을 넣는다.
- 마지막 장면에서는 명확한 결론보다 여운을 남기고 댓글을 달고 싶게 만든다.

{_TOPIC_RULES}

{_CATEGORY_RULE}

[시청 유지 장치] — 다음 요소를 최소 4개 이상 반드시 포함:
예상 밖의 한마디 / 숨겨진 사정 / 뒤늦게 알게 된 진실 / 돈 문제 / 자존심 문제 /
세대 차이 / 가족 간 오해 / 오래 참아온 감정 / 반전 / 의문 던지기(시청자가 궁금해서
계속 보게 만드는 질문을 중간중간 배치) / 기존 상식 뒤집기(당연해 보이는 상황을
반대로 뒤집는 전개) / 스포일러 활용(초반에 결말의 일부나 충격적인 결과를 먼저
암시하고, 그 과정을 궁금하게 만들기) / 도전 과제 부여(주인공이 이루려는 목표를
분명히 제시하고, 그걸 해내는지 끝까지 지켜보게 만들기)

{_STYLE_RULES}

[유튜브 최적화]
- 썸네일/영상 제목은 클릭하고 싶게, 15자 이내로.
- 장면 전환마다 긴장감을 유지하고, 중간중간 새로운 갈등을 추가한다.

{_BGM_GUIDE}

형식은 반드시 아래와 똑같이 지켜줘 (콜론, 대괄호, & 기호 포함):

RESOLUTION:
1920x1080

CATEGORY:
(위 [카테고리] 목록 중 하나)

CAST:
나레이터: 나레이터

BGM:
(위 BGM 목록 중 이 이야기 분위기에 맞는 파일명 하나)

THUMBNAIL_LONG:
(영상 제목, 15자 이내로 임팩트 있게)

THUMBNAIL_SHORTS:
(영상 제목, 15자 이내로 임팩트 있게)

[SCENE:elderly man sitting alone at empty dinner table, 1920x1080]
나레이터: 명절 전날 밤, 아버지는 혼자 밥상을 차려놓고 앉아 계셨습니다.&
숟가락을 들었다가 다시 내려놓기를 몇 번이나 반복하셨는지 모릅니다.&
자식들 목소리가 그리웠던 걸까요, 아니면 그날따라 유독 조용한 집 안이
낯설었던 걸까요.&아버지는 그렇게 한참을 창밖만 바라보고 계셨습니다.

[SCENE:another short scene description in English, 1920x1080]
나레이터: 대사 예시(실제로는 이보다 훨씬 길게, 200자 이상으로 써라).

...(위와 같은 형식으로 정확히 최소 22개 이상 반복, 절대 그보다 적게 쓰지 마.
첫 번째 SCENE 예시처럼 각 대사를 200자 이상으로 풍부하게 써라)...

절대 규칙 (매우 중요):
- [SCENE: ... ] 대괄호 안의 장면 묘사는 100% 영어로만 써야 한다. 한국어 단어를 단 하나도
  섞지 마라. (좋은 예: "old woman looking out window, empty room" / 나쁜 예: "거실에서 창밖을 보는 노인")
  이 영어 묘사는 실제 무료 스톡 사진 검색 API에 그대로 쿼리로 들어가기 때문에, 한국어가
  섞이면 사진을 하나도 못 찾는다. "cinematic", "8k" 같은 AI 이미지 생성용 수식어도 쓰지 말고,
  실제 사진으로 존재할 법한 담백한 묘사로 써라. 사람이 등장하는 장면은 반드시 "Korean" 또는
  "Asian"을 인물 묘사에 넣어라(예: "Korean elderly man sitting alone"). 이게 없으면 서양인
  사진이 검색돼서 한국 사연 정서와 안 맞는 장면이 나온다.
- [SCENE] 블록은 반드시 22개 이상이어야 한다. 다 쓴 뒤 스스로 개수를 세어보고, 22개
  미만이면 장면을 더 쪼개서 채워라.
- 나레이터: 뒤에 오는 대사는 반드시 200자 이상이어야 한다. 다 쓴 뒤 짧은 SCENE이
  있는지 스스로 검토하고, 있으면 상황 묘사나 속마음을 더 채워 넣어라. "대사 내용."
  같은 한두 문장짜리 짧은 대사로 씬 개수만 채우는 건 절대 금지다.
- 대사 안에서는 문장이 끝날 때마다(또는 호흡이 끊기는 지점마다) 반드시 & 로
  끊어라. & 는 자막이 한 줄씩 나뉘는 기준이라서, & 없이 여러 문장을 이어
  쓰면 자막 한 화면에 3~4문장이 통째로 떠서 안 보이는 자막이 된다. 한 &
  구간은 자막 한 줄 분량(대략 1문장, 40자 안팎)을 넘기지 마라.
- CATEGORY/CAST/BGM/THUMBNAIL_LONG/THUMBNAIL_SHORTS 섹션 형식은 정확히 지켜줘.
- PHOTO: 섹션은 생략해도 됨 (자동으로 스톡 사진이 채워짐).
- 답변을 절대 중간에 끊지 말고, 하나의 답변 안에서 끝까지 다 써줘.
- 규칙 설명이나 해설은 절대 출력하지 말고, 위 형식의 대본 내용만 출력해줘.
"""

SHORTS_SCRIPT_PROMPT = f"""\
{_PERSONA}

아래 형식 그대로, 유튜브 쇼츠(세로 영상)용 짧고 임팩트 있는 사연 대본을 작성해줘.
전체 낭독 시간이 30~50초 정도 되도록 [SCENE] 블록은 5~8개만 쓰고, 각 SCENE의 대사는
1~2문장으로 짧고 강하게 써줘. 서론 없이 첫 문장부터 바로 훅(hook)으로 시작해서 끝까지
안 넘기고 보게 만들어줘.

[핵심 목표]
- 첫 3초 안에 강한 갈등이나 충격적인 한마디로 시작한다 — 매번 같은 방식 말고
  의문 던지기(궁금한 질문으로 시작) / 스포일러 활용(충격적 결과를 먼저 던지고
  과정을 궁금하게 함) 등을 번갈아 써라.
- 노년층이 실제로 겪을 법한 현실적인 갈등 하나에만 집중한다 (롱폼처럼 여러 겹으로 안 벌림).
- 후반부(끝나기 직전)에 예상 못한 반전이나 숨겨진 사정 하나를 넣는다.
- 마지막 SCENE은 결론을 다 주지 말고 여운을 남기며 팔로우/댓글을 유도한다.

{_TOPIC_RULES}

{_CATEGORY_RULE}

{_STYLE_RULES}

{_BGM_GUIDE}

형식은 반드시 아래와 똑같이 지켜줘 (콜론, 대괄호, & 기호 포함):

RESOLUTION:
1080x1920

CATEGORY:
(위 [카테고리] 목록 중 하나)

CAST:
나레이터: 나레이터

BGM:
(위 BGM 목록 중 이 이야기 분위기에 맞는 파일명 하나)

THUMBNAIL_LONG:
(영상 제목, 15자 이내로 임팩트 있게)

THUMBNAIL_SHORTS:
(영상 제목, 15자 이내로 임팩트 있게)

[SCENE:elderly woman staring at phone in dark kitchen, 1080x1920]
나레이터: 짧고 강한 한 문장.

[SCENE:another short scene description in English, 1080x1920]
나레이터: 짧고 강한 한 문장.

...(위와 같은 형식으로 5~8개만)...

절대 규칙 (매우 중요):
- 첫 SCENE 대사가 곧 훅입니다 — 숫자, 반전, 질문 등으로 3초 안에 시선을 끌어야 함.
- [SCENE: ... ] 대괄호 안의 장면 묘사는 100% 영어로만 써야 한다. 한국어 단어를 단 하나도
  섞지 마라. 이 영어 묘사는 실제 무료 스톡 사진 검색 API에 그대로 쿼리로 들어가기 때문에,
  한국어가 섞이면 사진을 하나도 못 찾는다. 사람이 등장하는 장면은 "Korean" 또는 "Asian"을
  인물 묘사에 넣어라(예: "Korean woman looking out window") — 안 그러면 서양인 사진이
  검색돼서 한국 사연 정서와 안 맞는다.
- 절대 8개 넘게 늘리지 말고 5~8개로 짧게 끝내줘 — 롱폼처럼 길게 쓰지 마.
- 대사 안에서 문장이 끝날 때마다 & 로 끊어라. & 없이 여러 문장을 이어 쓰면
  자막 한 화면에 여러 문장이 통째로 떠서 안 보이는 자막이 된다.
- PHOTO: 섹션은 생략해도 됨 (자동으로 스톡 사진이 채워짐).
- 규칙 설명이나 해설은 절대 출력하지 말고, 위 형식의 대본 내용만 출력해줘.
"""

def _topic_override_rules(custom_topic: str) -> str:
    """디스코드 봇 등에서 사용자가 직접 준 주제로 [주제] 섹션을 갈아끼울 때 씁니다."""
    return f"""\
[주제]
이번엔 반드시 다음 주제로 이야기를 써줘: "{custom_topic}"
이 주제에서 벗어나지 말고, 주어진 소재를 중심으로 이야기를 전개해줘.

중요:
- 특정 인물을 무조건 악역으로 만들지 않는다. 양쪽 모두 이해할 수 있게 만들되,
  주인공은 억울한 감정을 느껴야 한다.
- 댓글에서 의견이 갈릴 수 있는 구조로 만든다."""


# ─────────────────────────────────────────────
# 구독/좋아요 유도(CTA) 자동 배치
# ─────────────────────────────────────────────

_CTA_LABELS = {
    "early": "영상 시작 직후(0~15초)",
    "middle": "중간(전체의 30~40% 지점)",
    "before_end": "결말 직전",
    "ending": "마지막 장면",
}
_CTA_EXAMPLES = {
    "early": "이 이야기, 끝까지 보시면 반전이 있습니다.",
    "middle": "이 이야기가 어떻게 끝나는지 궁금하시다면, 잠시만 더 함께해 주세요.",
    "before_end": "이 이야기가 마음에 남으셨다면, 좋아요와 구독으로 응원해 주세요.",
    "ending": "이 이야기가 마음에 남으셨다면, 좋아요와 구독으로 다음 이야기에도 함께해 주세요.",
}


def cta_instruction(settings: dict | None) -> str:
    """settings: {"early": bool, "middle": bool, "before_end": bool, "ending": bool}.
    켜진 위치에서만 자연스러운 CTA(구독/좋아요 유도) 한 문장을 나레이터 대사에
    녹여 넣으라는 지시문을 만듭니다. 초반(early)은 후킹을 방해하니 기본값 off."""
    on = [pos for pos, enabled in (settings or {}).items() if enabled]
    if not on:
        return "[구독 유도]\n이번 영상에는 구독/좋아요 유도 문구를 절대 넣지 마라."
    lines = [
        "[구독 유도]",
        "아래 위치에서만 자연스럽게 구독/좋아요를 유도하는 짧은 한 문장을 그 근처 "
        "나레이터 대사 끝에 자연스럽게 녹여 넣어라 (그 외 위치에는 절대 넣지 마라):",
    ]
    for pos in on:
        if pos in _CTA_LABELS:
            lines.append(f'- {_CTA_LABELS[pos]}: 예) "{_CTA_EXAMPLES[pos]}"')
    lines.append(
        "말투는 위 예시처럼 정중하고 담백하게 — 과장되거나 애원하듯 반복하지 마라. "
        "예시 문장을 그대로 베끼지 말고, 이번 이야기의 분위기와 그 장면 내용에 맞게 "
        "매번 다른 표현으로 자연스럽게 바꿔 써라."
    )
    return "\n".join(lines)


def _apply_cta(prompt: str, cta_settings: dict | None, anchor: str, count: int = 1) -> str:
    if not cta_settings:
        return prompt
    block = cta_instruction(cta_settings)
    return prompt.replace(anchor, f"{block}\n\n{anchor}", count)


_TITLE_PLACEHOLDER = "(영상 제목, 15자 이내)"


def _forced_title_rules(title: str) -> str:
    return f'정확히 이 제목 그대로 써라(글자 수 제한 무시): "{title}"'


_COMBO_LONG_ANCHOR = (
    "[1] 롱폼 대본 — [SCENE] 블록 최소 22개, 전체 낭독 시간 8분 이상, 각 SCENE 대사는\n"
    "반드시 200자 이상(3~5문장)으로 풍부하게, 시청 유지 장치 최소 4개 포함. 아래 형식 그대로:"
)
_SHORTS_STANDALONE_ANCHOR = "형식은 반드시 아래와 똑같이 지켜줘 (콜론, 대괄호, & 기호 포함):"


def _retry_hint_rules(reason: str) -> str:
    return (
        f"[재작성 경고] 방금 전 시도는 분량 기준 미달로 반려됐다 ({reason}).\n"
        "같은 실수를 반복하지 마라 — [SCENE] 블록을 22개보다 확실히 더 여유 있게 쓰고,\n"
        "각 씬의 나레이터 대사를 200자보다 눈에 띄게 길게(상황 설명+속마음+주변 묘사+대화를\n"
        "섞어서) 써서, 이번에는 전체 대사 분량이 최소 기준을 넉넉히 넘기게 하라."
    )


def combo_script_prompt(
    custom_topic: str | None = None,
    forced_title: str | None = None,
    cta_settings: dict | None = None,
    retry_hint: str | None = None,
) -> str:
    """COMBO_SCRIPT_PROMPT를 그대로 쓰거나,
    - custom_topic이 있으면 [주제] 섹션을 사용자가 지정한 주제로 바꿔치기하고
    - forced_title이 있으면 THUMBNAIL_LONG/THUMBNAIL_SHORTS 자리에 이미 골라둔
      제목을 그대로 쓰게 강제하고
    - cta_settings가 있으면 구독 유도 문구 배치 지시를 롱폼 섹션 앞에 추가하고
    - retry_hint(직전 실패 사유)가 있으면 롱폼 섹션 앞에 재작성 경고를 추가합니다.
      (재생성 시도가 매번 같은 프롬프트로 "다시 굴리기"만 하면 분량 미달이 반복되기
      쉬워서, 정확히 뭐가 부족했는지 알려주면 다음 시도가 첫 시도만에 통과할 확률이
      높아지고, 그만큼 MAX_REGEN_ATTEMPTS를 다 채우는 비싼 재생성 횟수가 줄어듭니다.)"""
    prompt = COMBO_SCRIPT_PROMPT
    if custom_topic:
        prompt = prompt.replace(_TOPIC_RULES, _topic_override_rules(custom_topic))
    if forced_title:
        prompt = prompt.replace(_TITLE_PLACEHOLDER, _forced_title_rules(forced_title))
    prompt = _apply_cta(prompt, cta_settings, _COMBO_LONG_ANCHOR)
    if retry_hint:
        prompt = prompt.replace(_COMBO_LONG_ANCHOR, f"{_retry_hint_rules(retry_hint)}\n\n{_COMBO_LONG_ANCHOR}")
    return prompt


def shorts_script_prompt(cta_settings: dict | None = None) -> str:
    """SHORTS_SCRIPT_PROMPT에 cta_settings가 있으면 구독 유도 지시를 추가합니다."""
    return _apply_cta(SHORTS_SCRIPT_PROMPT, cta_settings, _SHORTS_STANDALONE_ANCHOR)


def topic_idea_prompt(performance_summary: str = "", recent_titles: list[str] | None = None) -> str:
    """대본을 바로 쓰지 않고, 주제 하나(짧은 한 줄)만 먼저 골라달라는 프롬프트.
    제목 후보 생성 단계에 넘길 "주제 문자열"이 필요할 때 씁니다."""
    recent_block = ""
    if recent_titles:
        recent_block = (
            "\n[최근에 이미 쓴 제목들 — 아래와 겹치거나 너무 비슷한 소재는 피해라]\n"
            + "\n".join(f"- {t}" for t in recent_titles[-20:])
        )
    perf_block = f"\n{performance_summary}\n" if performance_summary else ""

    return f"""\
{_PERSONA}

아직 대본을 쓰지 말고, 다음 사연 영상 주제를 딱 하나만 한 줄로 정해줘.
{perf_block}{recent_block}

{_TOPIC_RULES}

{_CATEGORY_RULE}

[출력 형식]
아래 형식 그대로 두 줄만 출력해라. 다른 설명은 절대 쓰지 마라.
CATEGORY: (카테고리 하나)
TOPIC: (한 줄 주제 설명, 한 문장)
"""


def title_candidates_prompt(topic: str, n: int = 10) -> str:
    """주어진 주제로 제목 후보 n개 + 스스로 매긴 후킹 점수를 JSON으로 받는 프롬프트."""
    return f"""\
{_PERSONA}

다음 사연 주제로 유튜브 영상 제목 후보를 정확히 {n}개 만들어줘.

주제: "{topic}"

[제목 작성 공식]
[강한 상황] + [궁금증] + [결말 미공개]

좋은 예: "아들은 20년 동안 어머니에게 전화하지 않았습니다. 그런데 매일 새벽,
어머니의 휴대폰에는 같은 번호가 찍혔습니다."
나쁜 예: "오늘은 한 어머니의 감동적인 이야기를 소개해드리겠습니다." (약한 시작, 절대 금지)

[규칙]
- 결말을 제목에서 다 공개하지 마라. 궁금증을 남겨야 한다.
- 각 제목은 30자 이내로 짧고 강하게.
- {n}개 전부 서로 다른 각도(인물 중심 / 상황 중심 / 대사 인용 / 숫자·시간 강조 등)로
  다양하게 만들어라. 비슷비슷한 제목을 반복하지 마라.

[출력 형식]
아래 JSON 배열 형식으로만 출력해라. 코드블록(```)도 쓰지 말고, 설명도 절대 쓰지 마라.
hook_score는 "클릭하고 싶게 만드는 정도"를 스스로 냉정하게 0~100으로 매긴 점수다.

[{{"title": "...", "hook_score": 0}}, {{"title": "...", "hook_score": 0}}]
"""


def topic_batch_prompt(
    count: int,
    category: str | None = None,
    performance_summary: str = "",
    recent_titles: list[str] | None = None,
) -> str:
    """/주제생성용 — 아직 대본을 쓰지 않고 주제 N개를 한 번에 골라달라는 프롬프트."""
    recent_block = ""
    if recent_titles:
        recent_block = (
            "\n[최근에 이미 쓴 제목들 — 아래와 겹치거나 너무 비슷한 소재는 피해라]\n"
            + "\n".join(f"- {t}" for t in recent_titles[-30:])
        )
    perf_block = f"\n{performance_summary}\n" if performance_summary else ""
    category_line = (
        f'모든 주제는 반드시 "{category}" 카테고리에 해당해야 한다.'
        if category else
        f"각 주제마다 아래 목록 중 어울리는 카테고리를 하나씩 골라라: {', '.join(CATEGORIES)}"
    )

    return f"""\
{_PERSONA}

아직 대본을 쓰지 말고, 다음 사연 영상 주제를 정확히 {count}개 만들어줘.
{perf_block}{recent_block}

{category_line}
서로 겹치지 않는, 완전히 다른 {count}개의 이야기여야 한다.

{_TOPIC_RULES}

[출력 형식]
아래 JSON 배열 형식으로만 출력해라. 코드블록(```)도 쓰지 말고, 설명도 절대 쓰지 마라.
[{{"category": "...", "topic": "..."}}, ...] — 정확히 {count}개.
"""


def script_score_prompt(script_text: str) -> str:
    """완성된 롱폼 대본을 채점해달라는 프롬프트 (자동 검수용).
    쇼츠 대본은 별도로 채점하지 않습니다 — 콤보 응답 전체를 채점 대상으로
    넘기면 입력 토큰만 늘어날 뿐 실제로는 롱폼 기준(첫 15초 후킹 등)으로
    평가하므로, 호출부에서 롱폼 부분만 잘라서 넘깁니다."""
    return f"""\
아래는 완성된 유튜브 롱폼 사연 대본이다. 편집자 입장에서 냉정하게 채점해줘.

---
{script_text}
---

[채점 기준]
- hook: 첫 15초 안에 시청자를 붙잡는 힘 (0~100)
- emotion: 감정 몰입도 — 억지스럽지 않고 현실적인가 (0~100)
- ending: 결말의 여운과 댓글 유도력 (0~100)
- dropout_risk: 초반 이탈 위험도. "low" / "medium" / "high" 중 하나
- cta_excess: 구독·좋아요 유도 문구가 과하거나 어색하게 반복되는지. true/false

[출력 형식]
아래 JSON만 출력해라. 코드블록(```)도 쓰지 말고, 설명도 절대 쓰지 마라.
{{"hook": 0, "emotion": 0, "ending": 0, "dropout_risk": "low", "cta_excess": false}}
"""


# 롱폼/쇼츠 응답을 이 줄로 정확히 구분합니다 — 응답에 이 줄이 있으면 두 개의
# story.txt로 쪼개서 저장합니다 (수동 가져오기·API 자동 생성 양쪽 동일).
SPLIT_DELIMITER = "=====SHORTS====="

# 롱폼 + 쇼츠를 한 번의 프롬프트로 같이 받는 템플릿. 응답 하나에 두 대본이
# SPLIT_DELIMITER 로 나뉘어 들어오고, 각 대본에 RESOLUTION: 이 박혀 있어서
# 나중에 처리될 때 해상도도 알아서 맞춰집니다.
COMBO_SCRIPT_PROMPT = f"""\
{_PERSONA}

아래에 롱폼 대본과 쇼츠 대본을 하나의 답변 안에 순서대로 둘 다 작성해줘.
두 대본은 완전히 독립된 story.txt 형식이어야 하고, 정확히 "{SPLIT_DELIMITER}"
라는 구분선 한 줄로만 나눠줘 (그 줄에는 다른 텍스트를 절대 섞지 마).
롱폼과 쇼츠는 반드시 같은 사연(같은 주제)으로 써줘.

[핵심 목표]
- 첫 15초(쇼츠는 3초) 안에 강한 갈등, 배신감, 서운함, 충격적인 상황을 제시한다.
- 시청자가 "나라면 어떻게 했을까?"를 계속 생각하게 만든다.
- 노년층이 실제로 겪을 법한 현실적인 이야기여야 한다.
- 지나친 막장이나 범죄보다 인간관계의 미묘한 감정을 활용한다.
- 영상 후반부에는 예상하지 못한 반전이나 새로운 사실을 넣는다.
- 마지막 장면에서는 명확한 결론보다 여운을 남기고 댓글을 달고 싶게 만든다.

{_TOPIC_RULES}

{_CATEGORY_RULE}
같은 사연이니까 롱폼/쇼츠 둘 다 CATEGORY는 동일해야 함.

[시청 유지 장치] (롱폼 기준, 최소 4개 이상 반드시 포함):
예상 밖의 한마디 / 숨겨진 사정 / 뒤늦게 알게 된 진실 / 돈 문제 / 자존심 문제 /
세대 차이 / 가족 간 오해 / 오래 참아온 감정 / 반전 / 의문 던지기(시청자가 궁금해서
계속 보게 만드는 질문을 중간중간 배치) / 기존 상식 뒤집기(당연해 보이는 상황을
반대로 뒤집는 전개) / 스포일러 활용(초반에 결말의 일부나 충격적인 결과를 먼저
암시하고, 그 과정을 궁금하게 만들기) / 도전 과제 부여(주인공이 이루려는 목표를
분명히 제시하고, 그걸 해내는지 끝까지 지켜보게 만들기)

{_STYLE_RULES}

[유튜브 최적화]
- 썸네일/영상 제목은 클릭하고 싶게, 15자 이내로.
- 장면 전환마다 긴장감을 유지하고, 중간중간 새로운 갈등을 추가한다.

{_BGM_GUIDE}
같은 사연이니까 롱폼/쇼츠 둘 다 BGM은 동일해야 함.

[1] 롱폼 대본 — [SCENE] 블록 최소 22개, 전체 낭독 시간 8분 이상, 각 SCENE 대사는
반드시 200자 이상(3~5문장)으로 풍부하게, 시청 유지 장치 최소 4개 포함. 아래 형식 그대로:

RESOLUTION:
1920x1080

CATEGORY:
(위 [카테고리] 목록 중 하나)

CAST:
나레이터: 나레이터

BGM:
(위 BGM 목록 중 이 이야기 분위기에 맞는 파일명 하나)

THUMBNAIL_LONG:
(영상 제목, 15자 이내)

THUMBNAIL_SHORTS:
(영상 제목, 15자 이내)

[SCENE:elderly man sitting alone at empty dinner table, 1920x1080]
나레이터: 명절 전날 밤, 아버지는 혼자 밥상을 차려놓고 앉아 계셨습니다.&
숟가락을 들었다가 다시 내려놓기를 몇 번이나 반복하셨는지 모릅니다.&
자식들 목소리가 그리웠던 걸까요, 아니면 그날따라 유독 조용한 집 안이
낯설었던 걸까요.&아버지는 그렇게 한참을 창밖만 바라보고 계셨습니다.

[SCENE:another short scene description in English, 1920x1080]
나레이터: 대사 예시(실제로는 이보다 훨씬 길게, 200자 이상으로 써라).

...(위와 같은 형식으로 정확히 최소 22개 이상 반복, 절대 그보다 적게 쓰지 마.
첫 번째 SCENE 예시처럼 각 대사를 200자 이상으로 풍부하게 써라)...

{SPLIT_DELIMITER}

[2] 쇼츠 대본 — 위 롱폼과 같은 주제의 핵심 갈등 하나만 뽑아 30~50초로 압축.
[SCENE] 블록은 5~8개만, 대사는 1~2문장으로 짧고 강하게, 첫 문장이 3초 안에
시선을 끄는 훅이어야 함. 아래 형식 그대로:

RESOLUTION:
1080x1920

CATEGORY:
(롱폼과 동일한 카테고리)

CAST:
나레이터: 나레이터

BGM:
(롱폼과 동일한 BGM 파일명)

THUMBNAIL_LONG:
(영상 제목, 15자 이내)

THUMBNAIL_SHORTS:
(영상 제목, 15자 이내)

[SCENE:elderly woman staring at phone in dark kitchen, 1080x1920]
나레이터: 짧고 강한 한 문장.

[SCENE:another short scene description in English, 1080x1920]
나레이터: 짧고 강한 한 문장.

...(5~8개만)...

절대 규칙 (매우 중요):
- [SCENE: ... ] 대괄호 안의 장면 묘사는 롱폼·쇼츠 둘 다 100% 영어로만 써야 한다. 한국어
  단어를 단 하나도 섞지 마라. (좋은 예: "old woman looking out window, empty room" /
  나쁜 예: "거실에서 창밖을 보는 노인") 이 영어 묘사는 실제 무료 스톡 사진 검색 API에
  그대로 쿼리로 들어가기 때문에, 한국어가 섞이면 사진을 하나도 못 찾는다. "cinematic",
  "8k" 같은 AI 이미지 생성용 수식어도 쓰지 말고, 실제 사진으로 존재할 법한 담백한 묘사로 써라.
  사람이 등장하는 장면은 "Korean" 또는 "Asian"을 인물 묘사에 넣어라(예: "Korean elderly
  man sitting alone") — 안 그러면 서양인 사진이 검색돼서 한국 사연 정서와 안 맞는다.
- 롱폼의 [SCENE] 블록은 반드시 22개 이상이어야 한다. 다 쓴 뒤 스스로 개수를 세어보고,
  22개 미만이면 장면을 더 쪼개서 채워라.
- 롱폼의 나레이터: 뒤에 오는 대사는 반드시 200자 이상이어야 한다. 다 쓴 뒤 짧은
  SCENE이 있는지 스스로 검토하고, 있으면 상황 묘사나 속마음을 더 채워 넣어라.
  "대사 내용." 같은 한두 문장짜리 짧은 대사로 씬 개수만 채우는 건 절대 금지다.
- 롱폼·쇼츠 둘 다, 대사 안에서 문장이 끝날 때마다 & 로 끊어라. & 는 자막이
  한 줄씩 나뉘는 기준이라서, & 없이 여러 문장을 이어 쓰면 자막 한 화면에
  3~4문장이 통째로 떠서 안 보이는 자막이 된다. 한 & 구간은 자막 한 줄
  분량(대략 1문장, 40자 안팎)을 넘기지 마라.
- "{SPLIT_DELIMITER}" 구분선은 정확히 그 형태 그대로, 앞뒤에 다른 문장 붙이지 말고 한 줄에 단독으로 써줘.
- PHOTO: 섹션은 생략해도 됨 (자동으로 스톡 사진이 채워짐).
- 규칙 설명이나 해설은 절대 출력하지 말고, 위 형식의 대본 내용만 출력해줘.
"""
